<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="TextStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:gravity">center_horizontal</item>
        <item name="android:layout_gravity">center_horizontal</item>
        <item name="android:layout_marginTop">@dimen/halfMargin</item>
        <item name="android:layout_marginBottom">@dimen/halfMargin</item>
    </style>

    <style name="BigTextStyle" parent="TextStyle">
        <item name="android:textSize">@dimen/defaultTextSize</item>
    </style>

    <style name="ButtonStyle">
        <item name="android:layout_marginTop">@dimen/defaultMargin</item>
        <item name="android:layout_marginBottom">@dimen/defaultMargin</item>
        <item name="android:background">@color/colorPrimary</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:gravity">center</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:padding">@dimen/halfMargin</item>
    </style>

</resources>
